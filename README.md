# ON Music APP

A small starter project used to practice the Git/GitHub pull request workflow.

## What this is

This repo contains a tiny greeting utility with a test, so we have something
real to improve via a pull request.

## Usage

```bash
python3 greet.py "World"
```

## Running tests

```bash
python3 -m unittest test_greet.py
```
